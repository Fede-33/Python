/* Elabora una consulta que muestre solamente las ciudades cuyo nombre es “Alexandria”. 
   Para esta consulta deben usar la sentencia WHERE, recuerden que ahí es donde se colocan las condiciones.*/

SELECT * FROM ciudades c WHERE nombre = "Alexandria";