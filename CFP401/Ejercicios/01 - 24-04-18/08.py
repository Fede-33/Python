#Ingresar un número real y mostrar su raíz cuadrada:

from math import * # Importar la librería completa de funciones matemáticas 

num = float(input("Ingrese un número: "))

print ("La raíz cuadrada de", num, "es:", sqrt(num))
